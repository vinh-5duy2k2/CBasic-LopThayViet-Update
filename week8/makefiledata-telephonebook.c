#include <stdio.h>
#include <stdlib.h>
#include <string.h>

enum{
    NAME_LENGTH = 30,
    TEL_LENGTH = 15,
    EMAIL_LENGTH = 50,

    FIRSTNAME_LENGTH = 10,
    SURNAME_LENGTH = 10,
    LASTNAME_LENGTH = 10
}; //Standard size

